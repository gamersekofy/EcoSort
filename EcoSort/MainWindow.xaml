<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="EcoSort.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="EcoSort">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Height="36" Background="Transparent" Padding="12,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <FontIcon Glyph="&#xE15F;" FontSize="14" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Text="EcoSort" Margin="8,0,0,0" VerticalAlignment="Center" />
        </Grid>

        <NavigationView Grid.Row="1"
                        x:Name="AppNavigationView"
                        IsSettingsVisible="True"
                        IsBackButtonVisible="Visible"
                        BackRequested="AppNavigationView_BackRequested"
                        SelectionChanged="AppNavigationView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="HomeNavItem" Content="Home" Icon="Home" Tag="home" />
                <NavigationViewItem x:Name="ClassifyNavItem" Content="Classify" Icon="Camera" Tag="classify" />
                <NavigationViewItem x:Name="EducationNavItem" Content="Learn" Icon="Library" Tag="education" />
                <NavigationViewItem x:Name="CentersNavItem" Content="Centers" Icon="Map" Tag="centers" />
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</Window>
